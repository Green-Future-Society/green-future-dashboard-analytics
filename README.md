# green-future-analytics
Python Analytics middleware that fetches data from the backend service, prepares it for analytics, analyses the data from the reports and serves it back via an API interface.
